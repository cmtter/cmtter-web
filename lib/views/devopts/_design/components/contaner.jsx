const props = {
  ui: 'contaner',
  columnCount: 2,
  justify: 'start',
  style: {
    margin: '0 0 0 0px'
  }
}

const children = []

const slots = {}

const tag = 'contaner'

const tagText = 'contaner容器-用于排版页面元素'

export default {
  tag,
  props,
  slots,
  children,
  tagText
}

