const props = {
  ui: 'asteps',
  percent: 30,
  type: 'line',
  status: 'exception'
}

const children = []

const slots = {}

const tag = 'aprogress'

const tagText = 'progress进度条'

export default {
  tag,
  props,
  slots,
  children,
  tagText
}

