const props = {
  ui: 'input',
  ddtype: 'date | datetime | email | integer | number | percentage | phone',
  value: null,
  onUpdate_value: null,
  label: '输入框'
}

const children = []

const slots = {}

const tag = 'input'

const tagText = 'input输入框'

export default {
  tag,
  props,
  slots,
  children,
  tagText
}

