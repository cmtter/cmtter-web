import { useLoadMore } from 'vue-request';
/**
 * setup() {
    const { dataList, loading, loadingMore, loadMore } = useLoadMore(url, {
      listKey: 'results',
    });
    return {
      loading,
      loadingMore,
      dataList,
      loadMore,
    };
  },
});
 * 
 * 
 */
export default useLoadMore